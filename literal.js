const answer = {
    succees: ['Great!!', 'You did it', 'You Rock'],
    failure: ['Try again', 'Not there yet', 'try harder']
}

const makeList = (arr, arr1) => {

const successItems = [];

for (let i = 0; i < arr.length; i++){
    successItems.push(arr[i],)
}

const failureItems = [];

for (let i = 0; i < arr1.length; i++){
    failureItems.push(arr1[i],)
}

 console.log(successItems);
 console.log(failureItems);
}

makelist = makeList(answer.succees, answer.failure)

